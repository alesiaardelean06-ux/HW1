chosen scenario: Volunteer Impact Story Site
implemented requirements checklist:
-valid HTML5 structure on all pages;
-same navigation menu on all pages with working links to all required sections;
-data page includes a table with at least 4 data rows;
-form page includes more than 5 controls;
-one external stylesheet is used by all pages;
-HTML pages and CSS pass validation; 
-project is published online on GitHub Pages.
